// The Runvoid Book - Complete Chapter Content (EN & RU)

const bookData = {
  en: [
    {
      id: "intro",
      title: "Introduction",
      section: "Preface",
      content: `
<h1>The Runvoid Programming Language</h1>
<p class="lead">Welcome to <em>The Runvoid Book</em>. Runvoid is an innovative, dual-mode compiled programming language designed to unite the conversational readability of natural English with the blazing speed and compactness of raw x86_64 machine code.</p>

<div class="callout note">
  <strong>Version:</strong> This book covers <strong>Runvoid v0.2.0</strong>, featuring both the beginner conversational mode and the high-performance Pro Systems Mode.
</div>

<h2>Why Runvoid?</h2>
<p>Modern software development often forces an uncomfortable trade-off:</p>
<ul>
  <li><strong>Interpreted languages (Python, Ruby, JavaScript):</strong> Friendly and fast to prototype, but rely on virtual machines that consume massive RAM, introduce startup delays, and require heavy runtime installations.</li>
  <li><strong>Systems languages (C, C++, Rust):</strong> Offer bare-metal execution speed and micro-binaries, but feature steep learning curves, dense punctuation, and complex toolchains.</li>
</ul>

<p><strong>Runvoid breaks this dichotomy.</strong> Its syntax reads like plain conversational English: <code>say "Hello"</code>, <code>remember score = 100</code>, <code>repeat 5 times</code>, <code>for every item in backpack</code>. Yet beneath the hood, the compiler—built in <strong>Rust</strong>—translates code directly into <strong>pure x86_64 NASM assembly</strong>, assembling and linking it into standalone, dependency-free Linux ELF executables as small as <strong>15 KB</strong>.</p>

<h2>Who This Book Is For</h2>
<ul>
  <li><strong>Complete Beginners:</strong> Anyone learning their first programming language can grasp Runvoid's conversational keywords without getting stuck on curly-brace placement or pointer math.</li>
  <li><strong>Educators:</strong> Teach core algorithmic thinking without boilerplate or environment configuration nightmares.</li>
  <li><strong>Systems & DevOps Engineers:</strong> Generate ultra-compact, dependency-free command-line tools, kernel modules, or freestanding micro-binaries.</li>
</ul>
`
    },
    {
      id: "quickstart",
      title: "1. Quickstart: 5-Minute Tour",
      section: "Getting Started",
      content: `
<h1>1. Quickstart: 5-Minute Tour</h1>
<p>Let's write and run your very first Runvoid programs.</p>

<h2>Step 1: Your First Line of Code</h2>
<p>Create a file named <code>hello.rv</code> and write:</p>

<pre><code class="language-runvoid">say "Hello, brave new world!"</code></pre>

<p>Run it instantly with the compiler:</p>
<pre><code class="language-bash">runvoid run hello.rv</code></pre>
<p><strong>Output:</strong> <code>Hello, brave new world!</code></p>

<h2>Step 2: Remembering Values</h2>
<p>Instead of cryptic keywords like <code>var</code> or <code>let</code>, tell the computer to <code>remember</code>:</p>

<pre><code class="language-runvoid">remember player = "Alex"
remember score = 100

say "Player: {player}, Score: {score}"

score = score + 50
say "New Score: {score}"</code></pre>

<h2>Step 3: Asking for Input</h2>
<pre><code class="language-runvoid">remember name = ask "What is your name? "
say "Welcome to the game, {name}!"</code></pre>

<h2>Step 4: Decisions and Loops</h2>
<pre><code class="language-runvoid">remember coins = 75

if coins >= 100 {
    say "You can purchase the magical sword!"
} otherwise if coins >= 50 {
    say "You can purchase an iron shield."
} otherwise {
    say "Keep adventuring to find more coins."
}

repeat 3 times {
    say "Hooray!"
}</code></pre>
`
    },
    {
      id: "cli",
      title: "2. Compiler CLI & Tooling",
      section: "Getting Started",
      content: `
<h1>2. Compiler CLI & Tooling</h1>
<p>The <code>runvoid</code> binary provides a complete suite of developer tools right out of the box.</p>

<h2>Running Code Instantly (<code>run</code>)</h2>
<p>Compiles into temporary memory and executes immediately with zero artifacts left behind:</p>
<pre><code class="language-bash">runvoid run script.rv</code></pre>

<h2>Building Standalone Binaries (<code>build</code>)</h2>
<p>Produces an optimized, stripped native Linux ELF executable:</p>
<pre><code class="language-bash">runvoid build script.rv -o my_program
./my_program</code></pre>

<h2>Inspecting Assembly Output (<code>emit-asm</code>)</h2>
<p>View the raw x86_64 NASM assembly generated by the compiler:</p>
<pre><code class="language-bash">runvoid emit-asm script.rv</code></pre>

<h2>Built-in Code Formatter (<code>fmt</code>)</h2>
<pre><code class="language-bash">runvoid fmt script.rv       # print formatted code
runvoid fmt -w script.rv    # format file in-place</code></pre>

<h2>Scaffolding Project Templates (<code>new</code>)</h2>
<pre><code class="language-bash">runvoid new game flappy.rv    # 2D Screen canvas arcade game
runvoid new gui panel.rv      # Native desktop GUI window
runvoid new script app.rv     # Conversational beginner script</code></pre>

<h2>Terminal Cheat Sheet (<code>cheat</code>)</h2>
<pre><code class="language-bash">runvoid cheat</code></pre>
`
    },
    {
      id: "beginner-syntax",
      title: "3. Beginner Fundamentals",
      section: "Beginner Guide",
      content: `
<h1>3. Beginner Fundamentals</h1>

<h2>Printing Output: <code>say</code> and <code>say same</code></h2>
<ul>
  <li><code>say &lt;expr&gt;</code>: Prints with a trailing newline.</li>
  <li><code>say same &lt;expr&gt;</code>: Prints <em>without</em> moving to a new line (stays on the same line).</li>
  <li><strong>ANSI Colors:</strong> Add color keywords directly without escape codes:</li>
</ul>

<pre><code class="language-runvoid">say "Normal text"
say green "Build Succeeded!"
say red "Critical Error!"
say yellow "Warning: Low disk space"
say blue "Info: Connected to server"
say cyan "Notice: Check messages"
say magenta "Bonus: Achievement unlocked!"

say same "Progress: "
say same green "[100%]\n"</code></pre>

<h2>String Interpolation: <code>{...}</code></h2>
<p>Embed variables and math expressions directly within double-quoted strings:</p>
<pre><code class="language-runvoid">remember items = 4
say "You have {items} items. Double capacity is {items * 2}."</code></pre>

<h2>Conversational Logic & Comparisons</h2>
<table class="book-table">
  <thead>
    <tr><th>Word</th><th>Symbol</th><th>Meaning</th><th>Example</th></tr>
  </thead>
  <tbody>
    <tr><td><code>is</code></td><td><code>==</code></td><td>Equal</td><td><code>if score is 100</code></td></tr>
    <tr><td><code>is not</code></td><td><code>!=</code></td><td>Not equal</td><td><code>if health is not 0</code></td></tr>
    <tr><td><code>and</code></td><td><code>and</code></td><td>Logical AND</td><td><code>if alive and ready</code></td></tr>
    <tr><td><code>or</code></td><td><code>or</code></td><td>Logical OR</td><td><code>if key or lockpick</code></td></tr>
    <tr><td><code>not</code></td><td><code>not</code></td><td>Logical NOT</td><td><code>if not finished</code></td></tr>
  </tbody>
</table>
`
    },
    {
      id: "control-flow",
      title: "4. Control Flow & Loops",
      section: "Beginner Guide",
      content: `
<h1>4. Control Flow & Loops</h1>

<h2>Branching: <code>if</code>, <code>otherwise if</code>, <code>otherwise</code></h2>
<pre><code class="language-runvoid">remember temp = 22

if temp > 30 {
    say "It is hot outside!"
} otherwise if temp > 15 {
    say "The weather is pleasant."
} otherwise {
    say "Dress warmly!"
}</code></pre>

<h2>Fixed Repetition: <code>repeat</code></h2>
<pre><code class="language-runvoid"># Fixed 3 repetitions:
repeat 3 times {
    say "Repeating..."
}

# With an index counter:
repeat 5 as step {
    say "Current step: {step}"
}</code></pre>

<h2>Conditional Loop: <code>while</code></h2>
<pre><code class="language-runvoid">remember counter = 1
while counter <= 3 {
    say "Number {counter}"
    counter = counter + 1
}</code></pre>

<h2>Loop Controls: <code>stop</code> and <code>skip</code></h2>
<pre><code class="language-runvoid">repeat 10 as i {
    if i is 2 {
        skip  # advances to next iteration
    }
    if i is 6 {
        stop  # breaks out of loop
    }
    say i
}</code></pre>
`
    },
    {
      id: "lists",
      title: "5. Conversational Word Lists",
      section: "Beginner Guide",
      content: `
<h1>5. Conversational Word Lists</h1>
<p>Runvoid makes handling collections as intuitive as writing a list on paper.</p>

<pre><code class="language-runvoid"># Create a list:
remember inventory = "Torch", "Shield", "Health Potion"

# Add items:
add "Magic Ring" to inventory

# Remove items:
remove "Shield" from inventory

# Check membership:
if inventory has "Torch" {
    say "You have light in the dark!"
}

# Count items:
say "Total gear: {count inventory}"

# Iterate naturally:
for every item in inventory {
    say " - {item}"
}</code></pre>
`
    },
    {
      id: "media-gui",
      title: "6. 2D Canvas & Native GUI",
      section: "Graphics & UI",
      content: `
<h1>6. 2D Canvas & Native GUI</h1>

<h2>Hardware 2D Screen Canvas</h2>
<p>Runvoid includes built-in X11 hardware canvas rendering without external graphics engines:</p>

<pre><code class="language-runvoid">screen "Arcade 2D", 640, 480 {
    draw box at 0, 0, size 640, 480, color "black"
    draw circle at 320, 240, size 50, color "cyan"
    draw line from 0, 0 to 640, 480, color "red"
    draw text "Runvoid Canvas Engine", at 200, 50, color "yellow"
}</code></pre>

<h2>Declarative Desktop Windows (<code>window</code>)</h2>
<pre><code class="language-runvoid">window "Runvoid Control Panel", 450, 320 {
    label "Engine Settings:"
    checkbox "Enable Sound FX", 1
    checkbox "Fullscreen Mode", 0

    button "Save Configuration" {
        say "Configuration saved!"
    }
}</code></pre>
`
    },
    {
      id: "pro-mode",
      title: "7. Pro Systems Mode",
      section: "Systems Programming",
      content: `
<h1>7. Pro Systems Mode (<code>add Advanced</code>)</h1>
<p>When you demand zero runtime pauses, hardware registers access, and strict typing, Runvoid transforms into an uncompromising systems programming language.</p>

<h2>Directives & Activation</h2>
<pre><code class="language-runvoid">remove garbageC   # Disables GC runtime
remove Basic      # Enforces strict static types
add Advanced      # Unlocks systems intrinsics and optimizations</code></pre>

<h2>Strict Static Typing</h2>
<p>Types include <code>Int</code>, <code>String</code>, <code>Bool</code>, <code>Ptr</code>, <code>Void</code>, or custom structs:</p>
<pre><code class="language-runvoid">remember count: Int = 42
remember title: String = "Core"

action multiply(a: Int, b: Int): Int {
    give a * b
}</code></pre>

<h2>Modular Standard Library</h2>
<pre><code class="language-runvoid">use ior       # I/O: say, ask
use math      # Math: sqrt, sin, cos, pow, abs (-lm)
use sys       # Primitives: exit, getpid
use mem       # Direct memory: alloc, free
use fs        # Files: read, write, copy, delete
use net       # TCP sockets: tcp_listen, tcp_send, etc.
use thread    # POSIX threads (-lpthread)</code></pre>

<h2>Raw Pointers & Direct Memory</h2>
<pre><code class="language-runvoid">use mem

remember val: Int = 500
remember ptr: Ptr = addr val

say @ptr       // 500
@ptr = 750     // write through pointer
say val        // 750

remember heap: Ptr = alloc 64
@heap = 9999
free heap</code></pre>

<h2>Hardware Inline Assembly (<code>asm</code>)</h2>
<pre><code class="language-runvoid">asm {
    mov rax, 42
    imul rax, 10
}</code></pre>

<h2>CPU Cycle Benchmarking (<code>RDTSC</code>)</h2>
<pre><code class="language-runvoid">measure cycles {
    remember a: Int = 100
    remember b: Int = 200
    say a * b
}
// Outputs: ⏱️ Executed in 130 CPU cycles</code></pre>

<h2>Bare-Metal Freestanding Mode</h2>
<pre><code class="language-runvoid">remove garbageC
remove Basic
remove Linux
add Advanced
add Freestanding

# Direct Linux exit syscall (60)
asm {
    mov rax, 60
    xor rdi, rdi
    syscall
}</code></pre>
<p>Linked directly with GNU <code>ld -s</code>. Zero libc, zero runtime.</p>
`
    },
    {
      id: "reference",
      title: "8. Cheat Sheet & Reference",
      section: "Reference",
      content: `
<h1>8. Complete Keyword Reference</h1>
<p>Quick lookup table for all language keywords and directives:</p>

<table class="book-table">
  <thead>
    <tr><th>Keyword</th><th>Category</th><th>Usage</th></tr>
  </thead>
  <tbody>
    <tr><td><code>say</code></td><td>I/O</td><td><code>say "text"</code></td></tr>
    <tr><td><code>say same</code></td><td>I/O</td><td><code>say same "no newline"</code></td></tr>
    <tr><td><code>remember</code></td><td>Variable</td><td><code>remember x = 10</code></td></tr>
    <tr><td><code>ask</code></td><td>Input</td><td><code>remember name = ask "Name? "</code></td></tr>
    <tr><td><code>choose</code></td><td>Menu</td><td><code>choose "Title", "A", "B"</code></td></tr>
    <tr><td><code>if / otherwise</code></td><td>Control</td><td><code>if a { } otherwise { }</code></td></tr>
    <tr><td><code>repeat</code></td><td>Loop</td><td><code>repeat 5 times { }</code></td></tr>
    <tr><td><code>while</code></td><td>Loop</td><td><code>while cond { }</code></td></tr>
    <tr><td><code>for every ... in</code></td><td>List</td><td><code>for every item in list { }</code></td></tr>
    <tr><td><code>action / give</code></td><td>Function</td><td><code>action fn(x) { give x * 2 }</code></td></tr>
    <tr><td><code>screen</code></td><td>Graphics</td><td><code>screen "Game", 640, 480 { }</code></td></tr>
    <tr><td><code>window</code></td><td>GUI</td><td><code>window "App", 400, 300 { }</code></td></tr>
    <tr><td><code>addr / @</code></td><td>Pointers</td><td><code>addr val</code> / <code>@ptr</code></td></tr>
    <tr><td><code>alloc / free</code></td><td>Memory</td><td><code>alloc 64</code> / <code>free ptr</code></td></tr>
    <tr><td><code>asm</code></td><td>Hardware</td><td><code>asm { mov rax, 60 }</code></td></tr>
    <tr><td><code>thread</code></td><td>Threads</td><td><code>thread { atomic add x, 1 }</code></td></tr>
  </tbody>
</table>
`
    }
  ],

  ru: [
    {
      id: "intro",
      title: "Введение",
      section: "Предисловие",
      content: `
<h1>Язык программирования Runvoid</h1>
<p class="lead">Добро пожаловать в <em>Книгу о Runvoid</em>. Runvoid — это инновационный компилируемый язык программирования с двухуровневой архитектурой, объединяющий понятный синтаксис на естественном английском языке с максимальной скоростью чистого машинного кода x86_64.</p>

<div class="callout note">
  <strong>Версия:</strong> Данное руководство описывает <strong>Runvoid v0.2.0</strong>, включая новичковый разговорный режим и системный Pro Mode.
</div>

<h2>Почему именно Runvoid?</h2>
<p>Современные разработчики часто вынуждены выбирать между двумя крайностями:</p>
<ul>
  <li><strong>Интерпретируемые языки (Python, Ruby, JavaScript):</strong> Удобны и просты, но требуют тяжелых виртуальных машин, потребляют сотни мегабайт ОЗУ и долго запускаются.</li>
  <li><strong>Системные языки (C, C++, Rust):</strong> Дают максимальную скорость процессора и бинарники по 15 КБ, но сложны в освоении и перегружены спецсимволами.</li>
</ul>

<p><strong>Runvoid объединяет лучшее:</strong> синтаксис читается как живая речь: <code>say "Привет"</code>, <code>remember score = 100</code>, <code>repeat 5 times</code>, <code>for every item in backpack</code>. При этом компилятор на <strong>Rust</strong> транслирует код сразу в <strong>чистый ассемблер x86_64 NASM</strong>, собирая автономные бинарники Linux ELF размером от <strong>15 килобайт</strong> без виртуальных машин.</p>
`
    },
    {
      id: "quickstart",
      title: "1. Быстрый старт за 5 минут",
      section: "Начало работы",
      content: `
<h1>1. Быстрый старт за 5 минут</h1>

<h2>Шаг 1: Первая строчка кода</h2>
<p>Создайте файл <code>hello.rv</code>:</p>
<pre><code class="language-runvoid">say "Привет, прекрасный мир Runvoid!"</code></pre>
<p>Запустите прямо из консоли:</p>
<pre><code class="language-bash">runvoid run hello.rv</code></pre>

<h2>Шаг 2: Переменные («Запомни»)</h2>
<pre><code class="language-runvoid">remember player = "Алекс"
remember score = 100

say "Игрок: {player}, Очки: {score}"

score = score + 50
say "Новый счет: {score}"</code></pre>

<h2>Шаг 3: Ввод пользователя («Спроси»)</h2>
<pre><code class="language-runvoid">remember name = ask "Как зовут твоего героя? "
say "Добро пожаловать в игру, {name}!"</code></pre>

<h2>Шаг 4: Условия и ветвления</h2>
<pre><code class="language-runvoid">remember gold = 75

if gold >= 100 {
    say "Ты можешь купить легендарный меч!"
} otherwise if gold >= 50 {
    say "Золота хватает на прочный щит."
} otherwise {
    say "Копи монеты, искатель приключений."
}</code></pre>
`
    },
    {
      id: "cli",
      title: "2. Инструменты CLI",
      section: "Начало работы",
      content: `
<h1>2. Инструменты CLI компилятора</h1>

<h2>Мгновенный запуск скрипта (<code>run</code>)</h2>
<pre><code class="language-bash">runvoid run script.rv</code></pre>

<h2>Сборка автономного бинарника (<code>build</code>)</h2>
<pre><code class="language-bash">runvoid build script.rv -o my_app
./my_app</code></pre>

<h2>Вывод сгенерированного ассемблера (<code>emit-asm</code>)</h2>
<pre><code class="language-bash">runvoid emit-asm script.rv</code></pre>

<h2>Автоформатирование кода (<code>fmt</code>)</h2>
<pre><code class="language-bash">runvoid fmt script.rv       # просмотр форматирования
runvoid fmt -w script.rv    # форматирование на месте</code></pre>

<h2>Создание шаблонов проектов (<code>new</code>)</h2>
<pre><code class="language-bash">runvoid new game flappy.rv   # 2D холст для аркадной игры
runvoid new gui panel.rv     # Графическое окно
runvoid new script app.rv    # Разговорный скрипт</code></pre>
`
    },
    {
      id: "beginner-syntax",
      title: "3. Основы для начинающих",
      section: "Базовый синтаксис",
      content: `
<h1>3. Основы для начинающих</h1>

<h2>Печать текста: <code>say</code> и <code>say same</code></h2>
<ul>
  <li><code>say</code> — печатает текст с переводом на новую строку.</li>
  <li><code>say same</code> — печатает без перевода строки (оставаясь на той же).</li>
  <li><strong>ANSI-цвета:</strong> раскрашивайте вывод терминала встроенными словами:</li>
</ul>

<pre><code class="language-runvoid">say "Обычный текст"
say green "Сборка завершена успешно!"
say red "Критическая ошибка!"
say yellow "Предупреждение: мало памяти"
say blue "Информация: соединение установлено"

say same "Прогресс: "
say same green "[100%]\n"</code></pre>

<h2>Интерполяция строк: <code>{...}</code></h2>
<pre><code class="language-runvoid">remember items = 4
say "У тебя {items} предметов. Удвоение: {items * 2}."</code></pre>
`
    },
    {
      id: "control-flow",
      title: "4. Управление и циклы",
      section: "Базовый синтаксис",
      content: `
<h1>4. Управление и циклы</h1>

<h2>Ветвление: <code>if</code>, <code>otherwise if</code>, <code>otherwise</code></h2>
<pre><code class="language-runvoid">remember temp = 22

if temp > 30 {
    say "На улице жара!"
} otherwise if temp > 15 {
    say "Отличная комфортная погода."
} otherwise {
    say "Надень теплую куртку!"
}</code></pre>

<h2>Цикл повторения: <code>repeat</code></h2>
<pre><code class="language-runvoid">repeat 3 times {
    say "Ура!"
}

repeat 5 as step {
    say "Шаг: {step}"
}</code></pre>

<h2>Цикл <code>while</code> и прерывания</h2>
<pre><code class="language-runvoid">remember k = 1
while k <= 5 {
    if k is 2 {
        skip  # продолжить (continue)
    }
    if k is 4 {
        stop  # выйти (break)
    }
    say k
    k = k + 1
}</code></pre>
`
    },
    {
      id: "lists",
      title: "5. Разговорные списки",
      section: "Базовый синтаксис",
      content: `
<h1>5. Разговорные списки</h1>
<p>Работать со списками в Runvoid так же естественно, как вести список покупок:</p>

<pre><code class="language-runvoid">remember inventory = "Факел", "Щит", "Зелье здоровья"

add "Магическое кольцо" to inventory
remove "Щит" from inventory

if inventory has "Факел" {
    say "В подземелье будет светло!"
}

say "Всего предметов: {count inventory}"

for every item in inventory {
    say " - {item}"
}</code></pre>
`
    },
    {
      id: "media-gui",
      title: "6. Графика 2D и Окна",
      section: "Графика и GUI",
      content: `
<h1>6. Графика 2D и Настольные окна</h1>

<h2>Аппаратный 2D холст (Screen)</h2>
<p>Встроенное рисование 2D графики на X11 без громоздких внешних движков:</p>

<pre><code class="language-runvoid">screen "Ретро Игра", 640, 480 {
    draw box at 0, 0, size 640, 480, color "black"
    draw circle at 320, 240, size 50, color "cyan"
    draw line from 0, 0 to 640, 480, color "red"
    draw text "Runvoid Движок 2D", at 200, 50, color "yellow"
}</code></pre>

<h2>Графические десктопные окна (<code>window</code>)</h2>
<pre><code class="language-runvoid">window "Панель управления", 450, 320 {
    label "Настройки приложения:"
    checkbox "Звуковые эффекты", 1
    checkbox "Полный экран", 0

    button "Сохранить" {
        say "Настройки применены!"
    }
}</code></pre>
`
    },
    {
      id: "pro-mode",
      title: "7. Системный Pro-режим",
      section: "Системное программирование",
      content: `
<h1>7. Системный Pro-режим (<code>add Advanced</code>)</h1>
<p>Когда проекту требуется максимум контроля, нулевые паузы, аппаратные регистры процессора и строгая типизация — активируйте системный режим.</p>

<h2>Активация режима Pro</h2>
<pre><code class="language-runvoid">remove garbageC   # Отключает сборщик мусора
remove Basic      # Включает строгую типизацию
add Advanced      # Включает системный режим и оптимизации</code></pre>

<h2>Строгая статическая типизация</h2>
<pre><code class="language-runvoid">remember count: Int = 42
remember title: String = "Ядро"

action multiply(a: Int, b: Int): Int {
    give a * b
}</code></pre>

<h2>Модульная библиотека</h2>
<pre><code class="language-runvoid">use ior       # Ввод-вывод: say, ask
use math      # Математика: sqrt, sin, cos, pow, abs (-lm)
use sys       # Системные вызовы: exit, getpid
use mem       # Ручная память: alloc, free
use fs        # Файлы: read, write, copy, delete
use net       # Сетевые сокеты TCP
use thread    # Потоки ОС (-lpthread)</code></pre>

<h2>Сырые указатели и память</h2>
<pre><code class="language-runvoid">use mem

remember val: Int = 500
remember ptr: Ptr = addr val

say @ptr       // 500
@ptr = 750     // запись по адресу
say val        // 750

remember heap: Ptr = alloc 64
@heap = 9999
free heap</code></pre>

<h2>Встроенный ассемблер x86_64</h2>
<pre><code class="language-runvoid">asm {
    mov rax, 42
    imul rax, 10
}</code></pre>

<h2>Bare-Metal режим без ОС</h2>
<pre><code class="language-runvoid">remove garbageC
remove Basic
remove Linux
add Advanced
add Freestanding

asm {
    mov rax, 60
    xor rdi, rdi
    syscall
}</code></pre>
<p>Линкуется напрямую через <code>ld -s</code> без единого байта libc.</p>
`
    },
    {
      id: "reference",
      title: "8. Справочник ключевых слов",
      section: "Справочник",
      content: `
<h1>8. Полная таблица ключевых слов</h1>

<table class="book-table">
  <thead>
    <tr><th>Слово</th><th>Категория</th><th>Синтаксис</th></tr>
  </thead>
  <tbody>
    <tr><td><code>say</code></td><td>Ввод-вывод</td><td><code>say "текст"</code></td></tr>
    <tr><td><code>say same</code></td><td>Ввод-вывод</td><td><code>say same "в одну строку"</code></td></tr>
    <tr><td><code>remember</code></td><td>Переменные</td><td><code>remember x = 10</code></td></tr>
    <tr><td><code>ask</code></td><td>Ввод</td><td><code>remember name = ask "Имя? "</code></td></tr>
    <tr><td><code>choose</code></td><td>Меню</td><td><code>choose "Заголовок", "A", "B"</code></td></tr>
    <tr><td><code>if / otherwise</code></td><td>Условия</td><td><code>if a { } otherwise { }</code></td></tr>
    <tr><td><code>repeat</code></td><td>Цикл</td><td><code>repeat 5 times { }</code></td></tr>
    <tr><td><code>while</code></td><td>Цикл</td><td><code>while cond { }</code></td></tr>
    <tr><td><code>for every ... in</code></td><td>Списки</td><td><code>for every item in list { }</code></td></tr>
    <tr><td><code>action / give</code></td><td>Функции</td><td><code>action fn(x) { give x * 2 }</code></td></tr>
    <tr><td><code>screen</code></td><td>Графика</td><td><code>screen "Игра", 640, 480 { }</code></td></tr>
    <tr><td><code>window</code></td><td>GUI</td><td><code>window "Панель", 400, 300 { }</code></td></tr>
    <tr><td><code>addr / @</code></td><td>Указатели</td><td><code>addr val</code> / <code>@ptr</code></td></tr>
    <tr><td><code>alloc / free</code></td><td>Память</td><td><code>alloc 64</code> / <code>free ptr</code></td></tr>
    <tr><td><code>asm</code></td><td>Железо</td><td><code>asm { mov rax, 60 }</code></td></tr>
    <tr><td><code>thread</code></td><td>Потоки</td><td><code>thread { atomic add x, 1 }</code></td></tr>
  </tbody>
</table>
`
    }
  ]
};
